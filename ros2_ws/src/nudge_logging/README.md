# nudge_logging

Defines JSON schemas, telemetry columns, event names, and a small utility that
creates the required per-run directory skeleton. Telemetry publication, rosbag
orchestration, buffering, and persistence policy are deferred.
